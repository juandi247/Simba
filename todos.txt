TODOS FOR NOW:
FLUJOS:
- Entender el flujo general de commitear mensajes (commitIndex del lider)
- Entender el flujo de rechazo de mensaje. Que pasa si un nodo followe rtiene su log desactualizado O distinto, etnoces toca manejar el caso
        if prevLogIndex > len(log) OR log[prevLogIndex].term != prevLogTerm:
            reject

- Entender el flujo ahora de rechazo en una votacion, comparacoin de logs entre candidatos





 
TODOS GOOD: 

---- CORE RAFT----------

(X) Set up the interfaces for each message, and some methods to handle. (it fehlt the timeouts from both leader and heartbeat type) 

( ) Agregar el tipo de mensaje de ElectionTimeout para reiniciar la eleccion y hacer reinicio. 

( ) En el switch poner o tneer algo para handlers, especificos, que puedan tener validacion con un middleware para evaluar el TERM, asi evitamos ponerle el GetGerm a la interface, y simplificamos todo. 






----SIMULATOR-----------------------------------------
( ) Add an http server configuration, Server side events SSE for the UI so that it shows the update, also 
 the aproach for updating the data. Could be this json seralization or channel aproach idk

(X) Shuffle eligible events within a tick, seeded rng for reproducibility

( ) Checkear el node alive tambien para meterlo como mensaje para hacerle un shuffle




-----REAL LIFE------
( ) ON the real implmenetation, the worker pool will only, fire and wait for the acknowledgment that the messagearrived. 

( ) Its NOT that the goroutine wait for the response. 

( ) The one in charge of that is the server itself and then the response will come from the previous received as a new message.

( )  on the grpc call, we just need to send a type of NetworkMessage it will look something like: 
	NetworkMessage{
	typeOfMessage: type  (this is to know which type to cast)
	payload: bytes (this payload is the serialized data, coming from the struct used)
	} so that the fucntion of receving, will pass















que puedo hacer para poder ver el comportamiento de mis nodos. 
Algo fisico?, que avanza o se ve de alguna forma? CLI forma, o web forma????
